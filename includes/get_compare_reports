<?php
$con = mysqli_connect("localhost","root","forageme","db_forager");
if (mysqli_connect_errno())
{
  echo "Failed to connect to MySQL: " . mysqli_connect_error();
}
else{
	$report1 = $_POST['report1']; 
	$report2 = $_POST['report2'];

	$report1 = mysqli_real_escape_string($con,$report1);
	$report2  = mysqli_real_escape_string($con,$report2);

	//MySQL code goes here after tables are completed???
}
?>